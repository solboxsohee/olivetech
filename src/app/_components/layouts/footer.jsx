import React from 'react';

import {Layout} from 'antd';
const { Footer } = Layout;

export default function MainFooter() {
    return (
        <Footer>
            푸터
        </Footer>
    );
}

